# Ralph Progress Log
Started: Wed Mar 11 16:46:56 +04 2026
---

## Codebase Patterns
- Use `bun:sqlite` (`import { Database } from 'bun:sqlite'`) for all SQLite access — no external packages
- All schema migrations use `CREATE TABLE IF NOT EXISTS` for idempotency
- DB module lives at `src/db.ts` and exports the `db` instance; import with `import "./db"` in `index.ts` to ensure it runs on startup
- API route modules export a `const routes = { ... } as const` object that gets spread into `Bun.serve({ routes: { ...routeModule } })`
- With `noUncheckedIndexedAccess: true`, use `req.params["id"] ?? ""` instead of `req.params.id` to avoid TS errors on indexed access
- Use `db.prepare<Row, Params>("... RETURNING ...").get(...)` for INSERT/UPDATE that need the inserted/updated row back

## 2026-03-11 - US-001
- Created `src/db.ts` with `bun:sqlite` Database; opens/creates `budget.db`; creates `categories` and `transactions` tables with `CREATE TABLE IF NOT EXISTS`
- Added `import "./db"` to `src/index.ts` so schema runs on every server start
- Files changed: `src/db.ts` (new), `src/index.ts`
- **Learnings for future iterations:**
  - `bun:sqlite` is a built-in — no install needed, just `import { Database } from 'bun:sqlite'`
  - `AUTOINCREMENT` used on PKs to avoid ID reuse after deletes
  - Typecheck runs via `bunx tsc --noEmit`
---

## 2026-03-11 - US-002
- Created `src/api/categories.ts` exporting `categoryRoutes` with GET/POST on `/api/categories` and PUT/DELETE on `/api/categories/:id`
- Updated `src/index.ts` to import `categoryRoutes` and spread into `Bun.serve({ routes })`
- DELETE returns 409 if any transactions reference the category; uses `COUNT(*)` query before delete
- INSERT/UPDATE use `RETURNING` clause with `db.prepare().get()` to return the created/updated row
- Files changed: `src/api/categories.ts` (new), `src/index.ts`
- **Learnings for future iterations:**
  - `noUncheckedIndexedAccess: true` requires `req.params["id"] ?? ""` pattern for Bun route params
  - `db.prepare<ReturnType, ParamsType>("SQL RETURNING ...").get(args)` returns the row directly
  - Route modules work cleanly as `export const xRoutes = { "/path": { GET, POST } } as const` spread into serve routes
---

## 2026-03-11 - US-003
- Created `src/api/transactions.ts` exporting `transactionRoutes` with GET/POST on `/api/transactions` and DELETE on `/api/transactions/:id`
- GET supports optional `?month=YYYY-MM` query param using `LIKE 'YYYY-MM%'` filter on date column
- Response includes `category_name` via LEFT JOIN on categories table
- Updated `src/index.ts` to import `transactionRoutes` and spread into `Bun.serve({ routes })`
- Files changed: `src/api/transactions.ts` (new), `src/index.ts`
- **Learnings for future iterations:**
  - Use `new URL(req.url).searchParams.get("month")` to read query params in Bun route handlers
  - Month filtering: `WHERE t.date LIKE ?` with param `'YYYY-MM%'` works cleanly for ISO-8601 date strings
  - JOIN category name in the SELECT so frontend gets denormalized data in one request
---

## 2026-03-11 - US-004
- Updated `src/index.html` to add Tailwind CDN via `<script src="https://cdn.tailwindcss.com">` and point to `src/frontend/App.tsx`
- Created `src/frontend/App.tsx` as the new entry point with React tab navigation (Dashboard, Transactions, Categories)
- Created placeholder page components: `src/frontend/Dashboard.tsx`, `src/frontend/Transactions.tsx`, `src/frontend/Categories.tsx`
- Active tab uses `bg-indigo-100 text-indigo-700` Tailwind classes; inactive tabs use `text-gray-600`
- Tab switching is client-side React state — no full page reload
- Browser verified: all three tabs render and switch correctly
- Files changed: `src/index.html`, `src/frontend/App.tsx` (new), `src/frontend/Dashboard.tsx` (new), `src/frontend/Transactions.tsx` (new), `src/frontend/Categories.tsx` (new)
- **Learnings for future iterations:**
  - Frontend entry point moved from `src/frontend.tsx` to `src/frontend/App.tsx` — future UI stories should edit files in `src/frontend/`
  - Tailwind CDN added to `index.html`; all Tailwind classes work out of the box
  - HMR pattern from original `frontend.tsx` (`import.meta.hot.data.root`) preserved in new `App.tsx`
---

## 2026-03-11 - US-005
- Rewrote `src/frontend/Categories.tsx` with full CRUD UI: list table, inline add form, inline edit form, delete with 409 error handling
- Categories fetch on mount via `GET /api/categories`; list re-fetches after every create/update/delete
- Edit opens inline form pre-filled with current values; Save submits `PUT /api/categories/:id`
- Delete shows "Cannot delete: category has transactions" inline when API returns 409
- Monthly limit shows '—' when null
- Files changed: `src/frontend/Categories.tsx`
- **Learnings for future iterations:**
  - Use `void fetchX()` pattern in event handlers to avoid floating promise lint issues with async calls
  - Inline edit replaces the entire row via `editId === cat.id` conditional rendering — clean pattern for row-level editing
  - Browser verified: add, edit form, 409 delete error all work correctly
---

## 2026-03-11 - US-007
- Created `src/frontend/BudgetAlerts.tsx` component accepting `{ categoryName, limit, actual }[]` props; renders red alert per over-budget entry; returns null when none
- Updated `src/frontend/Dashboard.tsx` to fetch `/api/categories` and `/api/transactions?month=YYYY-MM`, sum spending per category, compute over-budget entries, and render `<BudgetAlerts>` at top above charts
- Browser verified: new Dashboard code is live; alerts hidden correctly when no over-budget categories
- Files changed: `src/frontend/BudgetAlerts.tsx` (new), `src/frontend/Dashboard.tsx`
- **Learnings for future iterations:**
  - `Promise.all([fetch(...), fetch(...)])` to load categories and transactions in parallel on mount
  - BudgetAlerts returns `null` (not an empty fragment) when no over-budget items — satisfies "no DOM output" requirement
  - Dashboard.tsx is the parent that owns data fetching; BudgetAlerts is a pure display component

---

## 2026-03-11 - US-006
- Rewrote `src/frontend/Transactions.tsx` with full list/add/delete UI
- Add form: amount (required), date (defaults to today via `new Date().toISOString().slice(0, 10)`), description (optional), category dropdown (populated from GET /api/categories)
- Table shows date, description, amount formatted as $X,XXX.XX via `toLocaleString("en-US", { style: "currency", currency: "USD" })`, category_name; ordered newest first from API
- Delete button removes row immediately via re-fetch after DELETE
- Files changed: `src/frontend/Transactions.tsx`
- **Learnings for future iterations:**
  - `toLocaleString("en-US", { style: "currency", currency: "USD" })` gives correct $X,XXX.XX format
  - Date input defaults to today with `new Date().toISOString().slice(0, 10)`
  - Category dropdown must be populated from a separate GET /api/categories fetch — both fetches happen in parallel via dual `useEffect` calls
  - Browser verified: add, list, delete all work correctly
---

## 2026-03-11 - US-008
- Installed recharts (`bun install recharts`)
- Created `src/frontend/SpendingChart.tsx`: bar chart of spending per category for current month using recharts `BarChart`; data fetched from `GET /api/transactions?month=YYYY-MM`; categories with zero spending excluded; chart title includes formatted month/year
- Updated `src/frontend/Dashboard.tsx` to import and render `<SpendingChart />` below `<BudgetAlerts>`
- Files changed: `src/frontend/SpendingChart.tsx` (new), `src/frontend/Dashboard.tsx`, `package.json`, `bun.lock`
- **Learnings for future iterations:**
  - recharts `Tooltip` `formatter` prop receives `ValueType | undefined` — must guard with `typeof value !== "number"` to satisfy TypeScript strict checks
  - Use `ResponsiveContainer width="100%" height={300}` to make recharts charts fill available width
  - `Cell` component inside `Bar` used to apply per-bar colors from a color palette array with `COLORS[index % COLORS.length]`
  - Manual browser verification needed (no browser tools available in this session)
---

## 2026-03-11 - US-009
- Created `src/frontend/MonthOverMonthChart.tsx`: bar chart of total spending per month for last 12 calendar months; months pre-generated client-side with zero amounts so no gaps appear; data fetched from `GET /api/transactions` (no filter); monthly aggregation done client-side via `tx.date.slice(0, 7)`
- Updated `src/frontend/Dashboard.tsx` to import and render `<MonthOverMonthChart />` below `<SpendingChart />`
- Files changed: `src/frontend/MonthOverMonthChart.tsx` (new), `src/frontend/Dashboard.tsx`
- **Learnings for future iterations:**
  - Generate last 12 months array upfront with `amount: 0` so all months have a bar even when no transactions exist
  - Use `new Date(year, month - i, 1)` iteration to generate prior months correctly (handles year boundary)
  - `tx.date.slice(0, 7)` extracts `YYYY-MM` from ISO-8601 date string for aggregation key
  - `labelFormatter` on Tooltip accepts `(label) => String(label)` to avoid raw node rendering
  - Manual browser verification needed (no browser tools available in this session)
---

## 2026-03-11 - US-010
- Created `src/frontend/CategoryBreakdownTable.tsx`: table with Category | Monthly Limit | Spent This Month | Remaining columns; rows sorted A–Z; 'No limit' when monthly_limit is null; '—' for Remaining when no limit; red text when Remaining is negative
- Updated `src/frontend/Dashboard.tsx` to build `CategoryRow[]` from already-fetched categories + transactions and pass to `<CategoryBreakdownTable rows={breakdownRows} />`; rendered below `<MonthOverMonthChart />`
- Files changed: `src/frontend/CategoryBreakdownTable.tsx` (new), `src/frontend/Dashboard.tsx`
- **Learnings for future iterations:**
  - Reuse already-fetched data in Dashboard rather than having the child component fetch independently — avoids duplicate requests
  - `[...rows].sort((a, b) => a.name.localeCompare(b.name))` is the clean pattern for alphabetical sort without mutating state
  - Manual browser verification needed (no browser tools available in this session)
---
